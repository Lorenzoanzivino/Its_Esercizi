# senza modulo:    min(minimo), max(massimo), abs(valore assoluto), pow(potenze)
# con il modulo:   sqrt(radice quadrata), ceil(arrotondare eccesso), floor(arrotondare difetto), pi(P-greco)

# x = 3 + 5    # 8
# y = x + 7    # 15
# z = ((x + y) * 2 + 35) / x    # 10.125

x = min(4, 0, 45)    # output: 0
y = max(4, 0, 45)    # output: 45


import math
x = math.sqrt(64)     # output 8
y = math.ceil(4.7)    # output 5
z = math.floor(4.7)   # output 4
w = math.pi           # output 3.141592653589793